{
  "name": "racetracker-tnc-relay",
  "version": "1.0.0",
  "description": "Local TNC relay agent for RaceTracker — bridges a KISS serial TNC to the RaceTracker server over plain HTTP/WebSocket.",
  "main": "tnc-relay.js",
  "scripts": {
    "start": "node tnc-relay.js"
  },
  "dependencies": {
    "serialport": "^12.0.0",
    "ws": "^8.0.0"
  }
}
